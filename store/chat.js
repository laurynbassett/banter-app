import React from 'react';

// ---------- ACTION TYPES ---------- //

// ---------- ACTION CREATORS ---------- //

// ---------- THUNK CREATORS ---------- //

// ---------- INITIAL STATE ---------- //
const defaultChat = {};

// ---------- REDUCER ---------- //
export default function(state = defaultChat, action) {
	switch (action.type) {
		default:
			return state;
	}
}
